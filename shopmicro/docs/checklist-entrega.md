# Checklist de evidencias

- Portada e índice automático.
- Diagrama de arquitectura.
- `docker-compose.yml` documentado en el PDF.
- `docker-stack.yml` documentado en el PDF.
- Manifiestos Kubernetes documentados en el PDF.
- Captura de `docker compose ps`.
- Captura de logs de servicios principales.
- Captura del flujo de consulta de productos.
- Captura del flujo de compra y notificación.
- Captura de Swarm con un worker caído y redistribución.
- Captura de `kubectl get pods -n shopmicro`.
- Captura de `kubectl get services -n shopmicro`.
- Comparativa final Compose vs Swarm vs Kubernetes.

